# Dépendances de développement (tests). Les scripts du dépôt tournent, eux, sans aucune
# dépendance : `python3 scripts/build_web.py _site` fonctionne avec Python seul.
pytest>=8

# Optionnelle, mais utilisée par le build du site pour produire les vignettes légères
# (scripts/derives.py) : sans elle, le site est construit avec les images d'origine.
# Les fetchers (scripts/images.py) s'en servent aussi pour réduire ce qu'ils téléchargent ;
# à défaut ils retombent sur `sips`, qui n'existe que sur macOS.
Pillow>=10

# Optionnelle elle aussi : relit le fichier .ics du rappel quotidien avec un parseur tiers
# (tests/test_rappel.py), plutôt que de s'en remettre à ma seule lecture du RFC 5545.
# Sans elle, ce test unique est sauté, les autres tournent.
icalendar>=6
